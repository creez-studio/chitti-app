export type Member={id:string;name:string;phone:string;user?:string;invite?:string;excluded?:string};
export type Payment={member:string;round:number;method:string;reference:string;status:'review'|'paid';date:string};
export type Draw={member:string;round:number;amount:number;date:string;by:string;manual:boolean;note:string};
export type Circle={id:string;name:string;owner:string;amount:number;months:number;paymentDate:string;drawDate:string;picker:string;upi:string;members:Member[];payments:Payment[];draws:Draw[];maintenance:boolean};
export function roundDate(date:string,round:number){const d=new Date(date+'T12:00:00');const day=d.getDate();d.setDate(1);d.setMonth(d.getMonth()+round-1);const last=new Date(d.getFullYear(),d.getMonth()+1,0).getDate();d.setDate(Math.min(day,last));return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}
export const sample:Circle={id:'demo',name:'Sneham Family Chitti',owner:'demo-owner',amount:2000,months:10,paymentDate:'2026-09-25',drawDate:'2026-09-28',picker:'m3',upi:'',maintenance:false,members:['Lakshmi Amma','Anjali Nair','Arjun Menon','Meera Nair','Rahul Krishnan','Devika Pillai','Vishnu Nair','Anu Menon','Sreedevi Amma','Kiran Kumar'].map((name,i)=>({id:'m'+i,name,phone:'',user:i===0?'demo-owner':undefined})),payments:[0,1,2,3,4,5,6].map(i=>({member:'m'+i,round:1,method:i%2?'UPI':'Cash',reference:i%2?'DEMO-2026-'+i:'',status:i===6?'review':'paid',date:'2026-09-22'})),draws:[]};
